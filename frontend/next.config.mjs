/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['picsum.photos', 'unsplash.com', 'images.pexels.com'],
    },
    // ... other configurations
};

export default nextConfig;
